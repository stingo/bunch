<div class="comments-header">  
  <h4>
    <spna id="post-comments-count"><%= commentable.comment_threads.count == 0 ? "Add" : commentable.comment_threads.count %></spna> Comments
<!--let profiles loggin to add comments-->
    <% if current_profile.blank? %> <%= link_to "Login to add comments", "#login", "data-toggle" => "modal", :class => 'btn btn-small' %> 
    <% else %>
     <% end %>
<!--end profil login-->

  </h4>
</div>

<%= render partial: "comments/form", locals: {new_comment: new_comment} %>

<div class="comments-container">  
  <%= render partial: "comments/reply", locals: {comments: commentable.root_comments, main: true} %>
</div>